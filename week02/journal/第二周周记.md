

# 第二周周记

## day1

### 1.1 项目进度

* 修改创建报修单功能，Controller层格外传入上传的图片的ids的list，在Service层根据ids来update图片的关联的repair_id
* 修改查看报修单详情功能，使传回数据有报修单的url的List
* 修改了token的生成逻辑，解决了可用期限的问题
* 将数据库连接参数、账号密码采⽤环境变量⽂件.env存储读取
* 用ai给每个需要详细记录日志的部分生成了log的语句（自己一个一个去手写真的会很麻烦！），并采用AOP来统一存储增删改的操作日志到数据库中，实现了日志的持久化

### 1.2 学习进度

* 边实战边巩固知识
* 在ai的帮助下将w1的代码迁移到了w2

### 1.3 明日目标

* 实现管理端⽇志查询功能
* 初步实现前后端的交互
***
## day2

### 1.1 项目进度

* 使全局异常处理器记录异常的堆栈日志
* 实现了管理端⽇志查询功能
* 使用ai创建了使用elementplus组件的Vue，实现了登录界面

### 1.2 学习进度

* 苦命努力看懂前端+解决跨域问题中

### 1.3 明日目标

* 把所有前端界面完成
***
## day3

### 1.1 项目进度

* 使用ai生成了项目有关的前端页面
  * 实现管理员多条件响应式查询
  * 美化了界面
* 为方便资源移植，使后端不存图片的完整地址，仅存图片名+后缀，然后在配置类中配置访问路径
* 把 `RepairRecordController `里 list 方法参数的 `@RequestBody(required = false)` 删掉了，因为Spring Boot 会自动把 GET 请求的 URL 参数映射到实体类中
* 把 拦截器 里的 `postHandle` 方法改成 `afterCompletion`方法，即使controller抛异常也能执行清除方法，以防ThreadLocal 数据无法被清掉
* 配置类配置跨域配置+拦截器预检请求放行
* 解决了字符串验null没验空串导致直接在mapper层抛异常的问题

### 1.2 学习进度

* 努力看前端的东西中

### 1.3 明日目标

* 开始学习Redis！
* 把重复的东西抽取出一个工具类，比如字符串校验和Integer校验，还有宿舍栋号不能为负这个也要验
***

## day4

### 1.1 项目进度
* 弄了一个`RepairRecordUtil`类来做输入参数的校验
### 1.2 学习进度

- 复习了可变参数
- 复习了Springboot的第三方依赖自动配置流程
- 复习了AOP的原理

* 反耳呢，我去学Redis了嘿嘿。今天把虚拟机配好了，然后学了各种命令。现在正在虚拟机上装Redis
### 1.3 明日目标
* 为了把token的过期与刷新做好，学好缓存功能，继续学习Redis
* 复习嵌套查询的基本实现语句和在mapper.xml中的配置方法
***
## day5

### 1.1 项目进度
* 无
### 1.2 学习进度

- 复习了嵌套查询和嵌套结果相关
- 学习了Jedis，SpringDataRedis的基本使用

### 1.3 明日目标
* 从RedisSerializer开始学，到完成黑马的短信登录+商户查询缓存两个案例
* 有余力可以开始想项目的Token的过期与刷新通过Redis缓存实现
***

## day6

### 1.1 项目进度
* 无
### 1.2 学习进度

- 复习了嵌套查询和嵌套结果相关
- 学习了Jedis，SpringDataRedis的基本使用

### 1.3 明日目标
* 了解了mybatis-plus的用法，知道自己应该把`pojo`分为`entity`和`dto`
* 了解了异常可以不要全抛`RuntimeException`，可以自定义异常，让全局处理器针对不同的异常返回不同的`HttpCode`
***