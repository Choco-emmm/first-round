

# 第二周周记

## day1

### 1.1 项目进度

* 修改创建报修单功能，Controller层格外传入上传的图片的ids的list，在Service层根据ids来update图片的关联的repair_id
* 修改查看报修单详情功能，使传回数据有报修单的url的List
* 修改了token的生成逻辑，解决了可用期限的问题
* 将数据库连接参数、账号密码采⽤环境变量⽂件.env存储读取
* 用ai给每个需要详细记录日志的部分生成了log的语句（自己一个一个去手写真的会很麻烦！），并采用AOP来统一存储增删改的操作日志到数据库中，实现了日志的持久化

### 1.2 学习进度

* 边实战边巩固知识
* 在ai的帮助下将w1的代码迁移到了w2

### 1.3 明日目标

* 实现管理端⽇志查询功能
* 初步实现前后端的交互
***
## day2

### 1.1 项目进度

* 使全局异常处理器记录异常的堆栈日志
* 实现了管理端⽇志查询功能
* 使用ai创建了使用elementplus组件的Vue，实现了登录界面
* 后端配置CORS（跨域资源）共享

### 1.2 学习进度

* 苦命努力看懂前端+解决跨域问题中

### 1.3 明日目标

* 把所有前端界面完成
***
## day3

### 1.1 项目进度

* 使用ai生成了前端页面
  * 实现响应式查询
* 使后端不存图片的完整地址，仅存图片名+后缀，然后在配置类中配置访问路径
* 把 `RepairRecordController.java` 里 `list` 方法参数的 `@RequestBody(required = false)` 删掉了，因为Spring Boot 会自动把 GET 请求的 URL 参数映射到实体类中
* 把 拦截器 里的 `postHandle` 方法改成 `afterCompletion`方法，即使controller抛异常也能执行，以防ThreadLocal 数据无法被清掉

### 1.2 学习进度

* ​

### 1.3 明日目标

* 用户登录后界面没有正确获取到localstorage里面的姓名
* 报修详情限制在有数据的表单的范围内展示了
***